{
  "name": "deeperseek",
  "version": "1.0.0",
  "description": "DeeperSeek Chat Application",
  "main": "server.js",
  "scripts": {
    "start": "npm install && node server.js",
    "build": "npm ci && cd frontend && npm ci && npm run build",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "better-sqlite3": "^8.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
